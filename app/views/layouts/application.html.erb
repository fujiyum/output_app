<!DOCTYPE html>
<html>
  <head>
    <title>OutputApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, shrink-to-fit=no">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript" src="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
    <style>.custom-file-label::after { content: "ファイル選択"; }</style>
  </head>

  <body>
    <header>
     <nav class="navbar navbar-expand-lg navbar-dark bg-white">
       <div class="container-fluid">
         <a class="navbar-brand text-dark" href="/">Monophyllaea</a>
         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
           <sapn class="navbar-toggler-icon text-dark"></sapn>
         </button>
         <div class="collapse navbar-collapse" id="navbarNavDropdown">
           <ul class="navbar-nav ml-auto">
           <% if user_signed_in? %>
            <li class="navbar-item">
              <%= link_to 'My page', user_path(current_user.id), class:'nav-link text-dark' %>
            </li>
            <li class="navbar-item">
              <%= link_to 'New Title', new_title_path, class:'nav-link text-dark' %>
            </li>
            <li class="navbar-item">
              <%= link_to 'Titles', titles_path, class:'nav-link text-dark' %>
            </li>
            <li class="navbar-item">
              <%= link_to 'Log out', destroy_user_session_path, method: :delete, class:'nav-link text-dark' %>
            </li>
           <% else %>
            <li class="navbar-item">
              <%= link_to 'Titles', titles_path, class:'nav-link text-dark' %>
            </li>
            <li class="navbar-item">
              <%= link_to 'Sign up', new_user_registration_path, class:'nav-link text-dark' %>
            </li>
            <li class="navbar-item">
              <%= link_to 'Log in', new_user_session_path, class:'nav-link text-dark' %>
            </li>
           <% end %>
           </ul>
         </div>
           <nav class="navbar navbar-light">
            <div class="form-inline container-fluid">
                <%= form_with url: search_users_path, method: :get, local: true do |f| %>
                <%= f.text_field :name, placeholder: "Username", class:"form-control", required:"required" %>
                <%= f.submit :search, class:"btn btn-outline-success" %>
                <% end %>
            </div>
           </nav>
       </div>
     </nav>
    </header>

    <main>
      <%= yield %>
    </mian>

    <footer class="text-dark">
        <div class="container-fluid d-flex align-items-center justify-content-center bg-light" style="height:100%;">
            <p class="footer-text text-center mb-0">
                Let him who would move the world, first move himself.
            </p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.js"></script>
    <script>
        bsCustomFileInput.init();
    </script>
  </body>

</html>
